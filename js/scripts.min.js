(function($) { $('html').removeClass('no-js'); var $header = $('header'); var $navLinks = $('header a[href^="#"]'); function setActiveNav() { var scrollPos = $(window).scrollTop() + 120; var current = ''; $navLinks.each(function() { var target = $(this).attr('href'); var $section = $(target); if ($section.length && $section.offset().top <= scrollPos) { current = target; } }); $navLinks.removeClass('active'); if (current) { $navLinks.filter('[href="' + current + '"]').addClass('active'); } } $('header a').click(function(e) { if ($(this).hasClass('no-scroll')) return; e.preventDefault(); var heading = $(this).attr('href'); var scrollDistance = $(heading).offset().top; $('html, body').animate({ scrollTop: scrollDistance + 'px' }, Math.abs(window.pageYOffset - $(heading).offset().top) / 1); if ($header.hasClass('active')) { $header.removeClass('active'); $('body').removeClass('active'); } }); $('#to-top').click(function() { $('html, body').animate({ scrollTop: 0 }, 500); }); $('#lead-down span').click(function() { var scrollDistance = $('#lead').next().offset().top; $('html, body').animate({ scrollTop: scrollDistance + 'px' }, 500); }); $('#experience-timeline').each(function() { var $this = $(this); var $userContent = $this.children('div'); $userContent.each(function() { $(this).addClass('vtimeline-content').wrap('<div class="vtimeline-point"><div class="vtimeline-block"></div></div>'); }); $this.find('.vtimeline-point').each(function() { var $content = $(this).find('.vtimeline-content'); var logo = $content.data('logo'); var company = $content.data('company'); if (logo) { $(this).prepend( '<div class="vtimeline-icon">' + '<img src="' + logo + '" alt="' + (company || 'Company') + ' logo">' + '</div>' ); } else { $(this).prepend('<div class="vtimeline-icon"><i class="fa fa-briefcase"></i></div>'); } }); $this.find('.vtimeline-content').each(function() { var date = $(this).data('date'); if (date) { $(this).parent().prepend('<span class="vtimeline-date">' + date + '</span>'); } }); }); $('#mobile-menu-open').click(function() { $header.addClass('active'); $('body').addClass('active'); }); $('#mobile-menu-close').click(function() { $header.removeClass('active'); $('body').removeClass('active'); }); $('#view-more-projects').click(function(e) { e.preventDefault(); $(this).fadeOut(300, function() { $('#more-projects').fadeIn(300); }); }); $(window).on('scroll', function() { if ($(window).scrollTop() > 80) { $header.addClass('sticky'); } else { $header.removeClass('sticky'); } setActiveNav(); }); setActiveNav(); if ('IntersectionObserver' in window && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) { var observer = new IntersectionObserver(function(entries) { entries.forEach(function(entry) { if (entry.isIntersecting) { entry.target.classList.add('visible'); observer.unobserve(entry.target); } }); }, { threshold: 0.15 }); document.querySelectorAll('.reveal').forEach(function(el) { observer.observe(el); }); } else { document.querySelectorAll('.reveal').forEach(function(el) { el.classList.add('visible'); }); } var yearEl = document.getElementById('year'); if (yearEl) { yearEl.textContent = new Date().getFullYear(); } var themeToggle = document.getElementById('theme-toggle'); var metaTheme = document.querySelector('meta[name="theme-color"]'); function applyTheme(theme) { if (theme === 'dark') { document.documentElement.setAttribute('data-theme', 'dark'); if (metaTheme) metaTheme.setAttribute('content', '#0f172a'); } else { document.documentElement.removeAttribute('data-theme'); if (metaTheme) metaTheme.setAttribute('content', '#0f172a'); } localStorage.setItem('theme', theme); } if (themeToggle) { themeToggle.addEventListener('click', function() { var isDark = document.documentElement.getAttribute('data-theme') === 'dark'; applyTheme(isDark ? 'light' : 'dark'); }); } })(jQuery);