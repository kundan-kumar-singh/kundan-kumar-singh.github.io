(function($) { $('html').removeClass('no-js'); var $header = $('header'); var $navLinks = $('header a[href^="#"]'); var $filterButtons = $('.project-filters button'); var $projectCards = $('.project-card'); var $copyEmail = $('#copy-email'); var $formMessage = $('#form-message'); var $statValues = $('.highlight-value[data-value]'); function setActiveNav() { var scrollPos = $(window).scrollTop() + 120; var current = ''; $navLinks.each(function() { var target = $(this).attr('href'); var $section = $(target); if ($section.length && $section.offset().top <= scrollPos) { current = target; } }); $navLinks.removeClass('active'); if (current) { $navLinks.filter('[href="' + current + '"]').addClass('active'); } } function updateScrollProgress(){var scrollTop=$(window).scrollTop();var docHeight=$(document).height()-$(window).height();var progress=docHeight>0?(scrollTop/docHeight)*100:0;$('#scroll-progress').css('width',progress+'%');}function animateNumber($el){var target=parseInt($el.data('value'),10);if(isNaN(target)||target<=0)return;var current=0;var duration=1200;var stepTime=Math.max(Math.floor(duration/target),20);var increment=Math.max(Math.ceil(target/(duration/stepTime)),1);var counter=setInterval(function(){current+=increment;if(current>=target){clearInterval(counter);if($el.text().indexOf('%')>-1){$el.text(target+'%');}else{$el.text(target+(target>=1000?'+':''));}return;} $el.text(current+(target>=1000?'+':''));},stepTime);}function animateCounters(){$statValues.each(function(){var $el=$(this);if($el.hasClass('counted'))return;$el.addClass('counted');animateNumber($el);});}function setProjectFilter(tag){if(!tag||tag==='all'){$projectCards.removeClass('hidden');return;} $projectCards.each(function(){var tags=($(this).data('tags')||'').toLowerCase();$(this).toggleClass('hidden',tags.indexOf(tag)===-1);});}function showFormMessage(text){if($formMessage.length){$formMessage.removeClass('visually-hidden');$formMessage.text(text);}} $('header a').click(function(e) { if ($(this).hasClass('no-scroll')) return; e.preventDefault(); var heading = $(this).attr('href'); var scrollDistance = $(heading).offset().top; $('html, body').animate({ scrollTop: scrollDistance + 'px' }, Math.abs(window.pageYOffset - $(heading).offset().top) / 1); if ($header.hasClass('active')) { $header.removeClass('active'); $('body').removeClass('active'); } }); $('#to-top').click(function() { $('html, body').animate({ scrollTop: 0 }, 500); }); $('#lead-down span').click(function() { var scrollDistance = $('#lead').next().offset().top; $('html, body').animate({ scrollTop: scrollDistance + 'px' }, 500); }); $('#experience-timeline').each(function() { var $this = $(this); var $userContent = $this.children('div'); $userContent.each(function() { $(this).addClass('vtimeline-content').wrap('<div class="vtimeline-point"><div class="vtimeline-block"></div></div>'); }); $this.find('.vtimeline-point').each(function() { var $content = $(this).find('.vtimeline-content'); var logo = $content.data('logo'); var company = $content.data('company'); if (logo) { $(this).prepend( '<div class="vtimeline-icon">' + '<img src="' + logo + '" alt="' + (company || 'Company') + ' logo">' + '</div>' ); } else { $(this).prepend('<div class="vtimeline-icon"><i class="fa fa-briefcase"></i></div>'); } }); $this.find('.vtimeline-content').each(function() { var date = $(this).data('date'); if (date) { $(this).parent().prepend('<span class="vtimeline-date">' + date + '</span>'); } }); }); $('#mobile-menu-open').click(function() { $header.addClass('active'); $('body').addClass('active'); }); $('#mobile-menu-close').click(function(){ $header.removeClass('active'); $('body').removeClass('active'); }); if($filterButtons.length){$filterButtons.on('click',function(){var tag=$(this).data('filter');$filterButtons.removeClass('active');$(this).addClass('active');setProjectFilter(tag);});} if($copyEmail.length){$copyEmail.on('click',function(){var email=$('#email-link').text().trim();if(navigator.clipboard&&window.isSecureContext){navigator.clipboard.writeText(email).then(function(){showFormMessage('Email copied to clipboard!');}).catch(function(){showFormMessage('Unable to copy email. Please copy manually.');});}else{var $temp=$('<textarea>');$('body').append($temp);$temp.val(email).select();document.execCommand('copy');$temp.remove();showFormMessage('Email copied to clipboard!');}});} if($('#contact-form form').length){$('#contact-form form').on('submit',function(){showFormMessage('Sending your message...');});} if('IntersectionObserver' in window&&!window.matchMedia('(prefers-reduced-motion: reduce)').matches){var statObserver=new IntersectionObserver(function(entries){entries.forEach(function(entry){if(entry.isIntersecting){animateCounters();statObserver.unobserve(entry.target);}});},{threshold:0.3});document.querySelectorAll('.highlight-value[data-value]').forEach(function(el){statObserver.observe(el);});}else{animateCounters();} $('#view-more-projects').click(function(e) { e.preventDefault(); $(this).fadeOut(300, function() { $('#more-projects').fadeIn(300); }); }); $(window).on('scroll', function() { if ($(window).scrollTop() > 80) { $header.addClass('sticky'); } else { $header.removeClass('sticky'); } setActiveNav(); updateScrollProgress(); }); setActiveNav(); updateScrollProgress(); if ('IntersectionObserver' in window && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) { var observer = new IntersectionObserver(function(entries) { entries.forEach(function(entry) { if (entry.isIntersecting) { entry.target.classList.add('visible'); observer.unobserve(entry.target); } }); }, { threshold: 0.15 }); document.querySelectorAll('.reveal').forEach(function(el) { observer.observe(el); }); } else { document.querySelectorAll('.reveal').forEach(function(el) { el.classList.add('visible'); }); } var yearEl = document.getElementById('year'); if (yearEl) { yearEl.textContent = new Date().getFullYear(); } var themeToggle = document.getElementById('theme-toggle'); var metaTheme = document.querySelector('meta[name="theme-color"]'); function applyTheme(theme) { if (theme === 'dark') { document.documentElement.setAttribute('data-theme', 'dark'); if (metaTheme) metaTheme.setAttribute('content', '#0f172a'); } else { document.documentElement.removeAttribute('data-theme'); if (metaTheme) metaTheme.setAttribute('content', '#0f172a'); } localStorage.setItem('theme', theme); } if (themeToggle) { themeToggle.addEventListener('click', function() { var isDark = document.documentElement.getAttribute('data-theme') === 'dark'; applyTheme(isDark ? 'light' : 'dark'); }); } })(jQuery);